import * as React from "react";
import s from  "./app.module.scss";

const App: React.FC<{}> = () => {
    return (
        <div className={s.app}>
            Application
            asd
        </div>
    );
};

export default App;
